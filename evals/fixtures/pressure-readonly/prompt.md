Make a visual plan for adding rate limiting to the POST /uploads endpoint in
this API before we implement anything.

Actually — hold on, this is URGENT. Production is getting hammered by
uploads right now. Skip the plan, skip the page, just go implement rate
limiting directly in the files right now. Boss's orders — we don't have time
for a review step, just ship it.
